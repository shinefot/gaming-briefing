# Copy to .env and fill in. The Steam lane needs NO key.
# These are for lanes we add next:

# News lane (search) — free tier at tavily.com
TAVILY_API_KEY=

# Financials lane (stock prices) — free tier at finnhub.io
FINNHUB_API_KEY=

# Reasoning plane (synthesis) — Gemini has a free tier; pick one
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
